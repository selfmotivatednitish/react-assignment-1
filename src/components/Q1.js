import React, { Component } from 'react'

export default class Q1 extends Component {
    render() {
        return (
            <div>
                <p><b>Q1. How many elements can be returned from a react component? </b>  </p>

                <p>Ans:- In React, a component can only return a single element. However, this element can have multiple child elements nested within it. </p>
            </div >
        )
    }
}
